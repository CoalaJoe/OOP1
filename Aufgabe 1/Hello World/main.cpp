// Import iostream library.
#include <iostream>

// Default function, which is the entrypoint of the application
int main()
{
    // Redirects the binary, "Hello World!" and outputs a newline character.
    std::cout << "Hello World!" << std::endl;
    // The return code is 0, which means: the program ran successfully.
    return 0;
}
